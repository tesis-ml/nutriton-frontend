import { AppWindow, ChartArea } from "lucide-react";


export const navigationConfig = [
    // Admin
    // Psycologist
    // Patient
    { to: "/dashboard", icon: AppWindow, label: "Zona de trabajo" },
    { to: "/stats", icon: ChartArea, label: "Estadísticas" },
];